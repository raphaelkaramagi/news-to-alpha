# Inference-only requirements for Railway (API-serving only, no training/scoring).
# Daily scoring runs on GitHub Actions — see .github/workflows/daily-update.yml.

# ── Core ────────────────────────────────────────────────────────────────────
pandas
numpy

# ── Env ─────────────────────────────────────────────────────────────────────
python-dotenv

# ── ML (rationale / ensemble explain only — no torch) ─────────────────────
scikit-learn>=1.6.0,<1.9.0   # must match training host when loading .joblib models
joblib

# ── Timezone / calendar ─────────────────────────────────────────────────────
pytz
exchange_calendars

# ── Web app ─────────────────────────────────────────────────────────────────
flask
gunicorn
